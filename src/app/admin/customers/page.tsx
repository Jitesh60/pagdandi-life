import { CustomersScreen } from "@/components/admin/customers-screen";

export const metadata = { title: "Customers" };

export default function AdminCustomersPage() {
  return <CustomersScreen />;
}
